import React from "react";

	const Button = ({clicked}) => {
		return <button onClick={clicked}>
		Click Here for a Random Board Game!!
		</button>;
	}

// Export Button Component
export default Button;
